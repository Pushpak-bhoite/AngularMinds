#include <stdio.h>
#include <math.h>

int main()
{
    int x = 3; // base
    double y = 2.1;  // power

    // using the pow() function
    double ans = pow(x, y);
    
    // printing the output
    printf("%f", ans);

    return 0;
}
